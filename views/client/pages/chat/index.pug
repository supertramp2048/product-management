extends ../../layouts/default.pug
block main 
  if(user)
    div
      .chat-container
          //- Message 1 (other)
          div(class="message user")
                img.avatar(src="")
                .bubble 
                  span.name #{"email"}
                    div(class="typing")
                      span
                      span
                      span
          div(class="chat p-2" my-id = user._id)
            each chat in chats
              div(class=(chat.user_id == user._id ? "message user" : "message other"))
                img.avatar(src=chat.infoUser.avatar)
                .bubble 
                  span.name #{chat.infoUser.email}
                  br
                  | #{chat.content}
          div(class="inner-list-typing")
      div(class="chat-input-container")
        .chat-input-box
            form(action=""  formSendMessage enctype="multipart/form-data")
              //- Upload file (đúng cấu trúc thư viện)
              div(class="custom-file-container" data-upload-id="my-unique-id")
                label Upload ảnh
                  a(class="custom-file-container__image-clear" href="javascript:void(0)" title="Clear") ×
                label(class="custom-file-container__custom-file")
                  input(type="file" class="hidden" class="custom-file-container__custom-file__custom-file-input" multiple)
                  span(class="custom-file-container__custom-file__custom-file-control")
                div(class="custom-file-container__image-preview")

              //- Nút emoji
              span(class="emoji-btn")
                i(class="fa-solid fa-face-grin-tongue-wink")
                div(class="tooltip" role="tooltip")
                  emoji-picker  

              //- Ô nhập nội dung + nút gửi
              input(type="text" name="content" placeholder="Nhập tin nhắn..." autocomplete="off")
              button(type="submit")
                i(class="fa-solid fa-paper-plane")
  else 
    h1 Bạn phải đăng nhập

  script(src="/socket.io/socket.io.js") 
  script(type="module" src="https://cdn.jsdelivr.net/npm/emoji-picker-element@^1/index.js")
