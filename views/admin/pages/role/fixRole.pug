extends ../../layouts/default.pug
include ../../mixins/alertSuccess.pug
block main
  +alertSuccess(3000)
  +alertFail(3000)
  div(class="container mx-auto p-6")
    h1(class="text-2xl font-bold text-gray-800 mb-6") Fix Role Group
    form(
        class="bg-white shadow-md rounded-lg p-6"
        action=`${prefixAdmin}/role/fixRoleGroupPatch/${role._id}?_method=PATCH`
        method="POST"
        id="form-create-role"
    )
      table(class="w-full table-auto")
        tbody
          tr(class="leading-10")
            td(class="w-1/3 font-semibold text-gray-700 py-3") 
              label(for="title") Title *
            td(class="py-3") 
              input(
                type="text" 
                id="title" 
                name="title" 
                value=role.title
                class="w-full border border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                placeholder="Enter product title"
              )
          tr(class="leading-10")
            td(class="font-semibold text-gray-700 py-3")
              label(for="thumbnail") Description
            td(class="py-3")
              textarea(
                name="description"
                id="description"
                class="w-full border border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                placeholder="descripttion of product "
                ) !{role.description}
          tr(class="leading-10")
            td(colspan="2" class="pt-6 text-right")
              button(
                type="button" 
                class="mr-3 px-4 py-2 text-gray-600 border border-gray-300 rounded-md hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-gray-500"
                onclick="window.location.href=`/admin/role`"
              ) Cancel
              button(
                type="submit" 
                class="px-6 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 disabled:opacity-50"
                id="submit-btn"
              ) Fix Role Group
  script(src="/admin/js/products.js")

  