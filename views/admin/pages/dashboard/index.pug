extends ../../layouts/default.pug
include ../../mixins/alertSuccess.pug

block main 
  if(rolesCurrent.permission.length > 0)
    +alertSuccess(3000)
    h1 Trang tong quan
      p #{user}
      p ------------------
      p= JSON.stringify(rolesCurrent)
  else 
    h1 Bạn không thể xem trang này